#### How modules should be implemented ####
- all functions (linear, ramp, jump, etc) must read in values using memory mapped IO
- determine what protocol/interface to use to communicate between the main controller and the peripherals (possibly use wishbone interface?) 
#### Modules to implement ####
# 

